[project]
name = "stromboli"
version = "0.2.0"
description = "Stromboli — an explicit LangGraph state machine for agentic coding triage."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "langgraph>=0.2",
    "langgraph-checkpoint-sqlite>=2",
    "litellm>=1.40",
    "claude-agent-sdk>=0.1",
    "chromadb>=0.5",
    "langfuse>=2",
    "httpx>=0.27",
    "pydantic>=2.6",
    "pydantic-settings>=2.2",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "ruff>=0.5",
    "mypy>=1.10",
    "prefect>=3",
]
# Orchestrator comparison (A): wrap the triage flow in Prefect.
orchestration = [
    "prefect>=3",
]
# GEPA/DSPy verifier prompt optimization (self-improving §2). Offline only —
# the runtime graph never imports DSPy; verifier_optimize loads it lazily.
optimize = [
    "dspy-ai>=2.5",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/stromboli"]

[tool.ruff]
line-length = 100
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "W"]

[tool.mypy]
python_version = "3.12"
strict = true
files = ["src", "tests"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
# Tests are functions, never classes — so domain models named ``Test*`` (e.g.
# ``TestResult``) are not mistaken for test cases.
python_classes = ["*Tests"]

[dependency-groups]
dev = [
    "pyyaml>=6.0.3",
    "types-pyyaml>=6.0.12.20260518",
]
