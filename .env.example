# Stromboli configuration — copy to `.env` and fill in real values.
# Every variable below is REQUIRED; the worker fails fast on startup naming any
# that is missing. `.env` is gitignored — never commit real secrets.

# Notion integration token (internal integration secret).
NOTION_TOKEN=

# GitHub token used to clone repos, push branches, and open PRs.
GITHUB_TOKEN=

# Langfuse observability credentials and host.
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_HOST=https://cloud.langfuse.com

# Public URL of the Cloudflare Tunnel that fronts the dispatch endpoint.
TUNNEL_PUBLIC_URL=

# Absolute path under which per-task git worktrees are created.
WORKSPACE_ROOT=

# Anthropic API key used by the Claude Code (Ralph) loop.
ANTHROPIC_API_KEY=

# Shared secret required in the dispatch request header.
DISPATCH_SHARED_SECRET=

# Optional: which build engine to use — "ralph" (default) or "graph".
# Leave unset for the legacy Ralph loop; set to "graph" for the multi-agent
# build engine (planner/worker/verifier/reflector).
STROMBOLI_ENGINE=ralph

# Optional: also write logs (and tracebacks) to this file, in addition to
# stderr. Leave unset to log to the console only.
STROMBOLI_LOG_FILE=

# Telegram alerts (optional). Both must be set to enable push notifications.
# These are 1Password references — resolve at deploy time with
# `op inject -i .env.example -o .env` or run the worker under `op run`.
TELEGRAM_BOT_TOKEN=op://Dev-Secrets/telegram-sys-ops-alert/credential
TELEGRAM_CHAT_ID=op://Dev-Secrets/telegram-sys-ops-alert/username
