Ralph rundown — run ralph-20260623-104222 — generated Tue 23 Jun 2026 11:07:30 AEST

================================================================
RALPH RUNDOWN
================================================================
Stromboli  ·  branch ralph/stromboli-v1  ·  K=3

PROGRESS
[█████████████···········]  53.8%  (7/13 done)
legend: █ done   ▒ blocked (remaining, not actionable)   · to-do

  A Foundation                     [████████████████] 100.0%  (2/2 done)
  B Trigger                        [████████████████] 100.0%  (2/2 done)
  C Execution                      [████████████····]  75.0%  (3/4 done)
  D Verify & Integrate             [················]   0.0%  (0/3 done)
  E Write-back & Observability     [················]   0.0%  (0/2 done)

UP NEXT (active — blocked items excluded)
  ☐ US-008 — Add a circuit breaker with configurable max-iterations and max-cost (USD) ceilings per task: on trip, stop the loop, set Status=Review, append a note explaining the trip to the task body, and log/trace the reason in Langfuse. Unit test: simulated cost over the ceiling trips the breaker and routes to Review (assert resulting status and note). Tests pass; typecheck and lint pass.
  ☐ US-009 — Add a reusable GitHub Actions workflow that runs on pull_request to install deps and run tests, lint, and type checks, failing the check when any fail; wire it into the target repo so it runs on the worker's branch; document in README how a new target repo opts in. CodeQL and the eval suite are explicitly deferred. Workflow YAML validates; typecheck and lint pass.
  ☐ US-010 — After a successful loop, commit the worktree changes, push the branch, open a PR against main with a title/body derived from the task and spec, and write the PR URL to the task PR field; if no changes were produced, route to Review with an explanatory note instead of opening an empty PR. Unit test verifies PR title/body derivation and the empty-diff branch. Tests pass; typecheck and lint pass.
  ☐ US-011 — Implement integration routing per the autonomy table: if Needs review==true set Status=Review; if Needs review==false set Status=Complete after the PR is opened (human merges in v1, no auto-merge); Assigned to==Human tasks are never picked up or modified. Unit test covers all three routing branches. Tests pass; typecheck and lint pass.
  ☐ US-012 — Write the worker outcome back to Notion: on completion (success, blocked, or breaker trip) append a feedback summary to the task body (work done, blockers and why, PR link) and set the final Status from US-011; make write-back resilient so a Notion write failure is retried and logged without crashing the worker. Unit test asserts the summary includes blocked-item reasons when present. Tests pass; typecheck and lint pass.
  ☐ US-013 — Add Langfuse observability: each task build is a trace and each Ralph iteration is a span; record cost and token usage parsed from CC output on the trace; tag circuit-breaker trips and failures. Writing Cost/Tokens back to Notion is deferred to phase 2. Tests pass; typecheck and lint pass.

BLOCKED / SKIPPED (0)
  (none)

DONE (7/13)
  US-001, US-002, US-003, US-004, US-005, US-006, US-007

RECENT PROGRESS
on the completion signal, termination on the ceiling) are covered directly, plus the third real exit (no-eligible, with and without any CC call).
  - Eligibility is re-read from disk each pass because the *agent* mutates the worktree's prd.json — the loop doesn't track item state itself. The stubbed CC in tests flips items via an `on_call` side effect to simulate this; that's the integration seam US-008/US-010 build on.
  - Ceiling vs no-eligible ordering: the eligibility check is at the TOP of each pass, so after the last iteration flips everything to pass, the next pass returns NO_ELIGIBLE (not CEILING) — the loop reports the *real* reason it ended. A stub that never flips items + never signals exercises the pure ceiling path.
  - CC is invoked via an injected `CCRunner = Callable[[Sequence[str], Path], str]` (argv, cwd) -> stdout, mirroring worktree's `GitRunner` seam — same `# noqa: S603` on the default subprocess shell-out. Never touch a real `claude` in tests.
  - `total_cost_usd` is captured per iteration now (US-007 only needs to *capture* JSON) so US-008's circuit breaker can sum cost across iterations and US-013 can put it on a Langfuse trace without re-running anything.

Cost: $0.00 spend so far · $0.00/loop burn rate · $0.00 per green
================================================================
